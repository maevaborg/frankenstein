Raccourcis jupyter python 3

echap + m = cellule txt
echap + d,d = supprimer une cellule
echap + a = ajout d'une cellule haut
echap + b = ajout d'une cellule bas
shift+entrée = excecuter le code nouvelle entrée

Entrée[*] = je cherche 
Entrée[*] (trop de temps) = j'ai planté, reboot-moi
-------------------
Python 3

pip = installer une librairie
import = importer
from 
encoding : nombre de version qu'on veut 
join = transforme en chaine de charactère le resultat 

RegexpTokenizer = ponctuation

 
if 
for 
in 
not in 
len 
set

------
!wget programme linux requete http / cmde dans jupyter
rb = ouvrir en mode binaire 

"correspondance par index" = trois lsites avec mm index 


------

pour lancer dans le terminal un fichier python : python scrap.py

installer un package : pip install 

appel d'une librairie pour un script plus intelligent = sys 
argv = element renvoie une liste pour recuperer ces elements
str = chaine de caractere
int = nombre entier 



exercice : ouvrir le fichier enlever les charactere speciaux, espaces, dont retour à la ligne, une seule chaine de caractere 